#include <stdio.h>
#include <stdlib.h>

/**
 * main - check the code
 * @argc: numbre of commands
 * @argv: Commands array
 * Return: Always 0.
 */
int	main(int argc, char *argv[])
{
if (argc)
{
if (argv[2] || !argv[1])
{
printf("Error\n");
}
}
return (0);
}
