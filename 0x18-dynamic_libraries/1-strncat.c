#include "main.h"

/**
 * _strncat - check the code
 * @dest: destination
 * @src: source
 * @n: max number of chars to append
 * Return: Always 0.
 */
char *_strncat(char *dest, char *src, int n)
{
	return (strncat(dest, src, n));
}
