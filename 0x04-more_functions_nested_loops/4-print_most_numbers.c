#include "main.h"

/**
 * print_most_numbers- check the code
 * I lied again
 * Return: Always 0.
 */
void print_most_numbers(void)
{
	int i;
		for (i = 48; i <= 57; i++)
		{
		if (i != 50 && i != 52)
		{
			putchar(i);
		}
		}
		putchar(10);
}
